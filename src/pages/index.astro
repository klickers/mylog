---
import Layout from "../layouts/Layout.astro"
import LogEntries from "../components/LogEntries"

const name = import.meta.env.PUBLIC_USER_NAME
---

<Layout title={`${name}'s Activity Log`}>
	<LogEntries client:load />
</Layout>
